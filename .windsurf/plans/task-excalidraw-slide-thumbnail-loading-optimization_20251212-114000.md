# 任务列表：Excalidraw 幻灯片缩略图加载优化（跨 Tab 缓存 + 按需优先生成）

## 当前任务
- [ ] 1. 抽离缩略图缓存：将 `thumbnailCache` 从 `PresentationMenu` 组件生命周期移出，保证切换 tab 不丢失缓存（必要时加简单上限/淘汰）。
- [ ] 2. 引入按需优先生成：实现缩略图生成队列（去重、并发控制、过期取消），进入 tab 后不再阻塞全局 loading。
- [ ] 3. 支持 activeSlideId 优先：点击画布 frame 导致列表滚动定位时，立即触发该 slide 的缩略图生成并尽快显示。
- [ ] 4. 降低缩略图生成成本（可选）：缩小导出尺寸/调整输出格式，确保“立即出现”更可达。
- [ ] 5. 自测回归：
  - 切换 tab 多次不再长时间 loading；
  - 点击画布 frame（尤其列表后部）定位后缩略图立即出现；
  - 拖拽排序/保存/开始播放不受影响。

## 已完成任务
- [x] 创建并确认 plan 文件

## 备注
- 本轮不做列表虚拟化。
- 若需要前端联调/验证，使用 Playwright 打开已有页面（不启动新服务、不占用新端口）。
